import { Fit, RepoRanger } from '../components'
import { Background } from '../layouts'

<Fit>Automate</Fit>
<Fit caps={true} style={{ color: `black`, backgroundColor: `white` }}>all</Fit>
<Fit>the things.</Fit>

---

<Fit>✅ CI Checks</Fit>

---

export default Background({ background: 'white' })

<img src="images/ci-checks.png" style={{ maxHeight: '75vh' }} />

---

export default Background({ background: `green`, color: `white` })

<Fit><code>bot: merge on green</code></Fit>

---

export default Background({ background: 'white' })

<img src="images/oss-bot.png" style={{ maxHeight: '75vh' }} />

---

export default Background({ background: `#2093f3` })

<RepoRanger style={{ width: '50vw' }} />